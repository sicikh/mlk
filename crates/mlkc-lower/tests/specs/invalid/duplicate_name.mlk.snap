---
source: crates/mlkc-lower/tests/spec_test.rs
expression: snapshot
---
## Input

```mlk
// A module that declares one name twice: the first declaration is what the name means, and
// the second is what the lowering reports. A name declared in another namespace is not a
// duplicate of it, which is what `valid/namespaces.mlk` shows.

type Int

fun size(value: Int): Int =
    value

fun size(value: Int): Int =
    value

type Point

type Point

```

## Item Tree

```
MODULE #0

ITEM TREE
  type Int  @2.0
    visibility: private
  fun size  @2.1
    visibility: private
    param: Int -> type Int
    ret: Int -> type Int
  fun size#1  @2.2
    visibility: private
    param: Int -> type Int
    ret: Int -> type Int
  type Point  @2.3
    visibility: private
  type Point#1  @2.4
    visibility: private
  use Int  prelude
    path: std.prelude.Int
    visibility: private
  use Unit  prelude
    path: std.prelude.Unit
    visibility: private
```

## Bodies

```
BODY fun size in module #0

root expr#0
params
  pat#0
exprs
  expr#0  path path#0
pats
  pat#0  bind value
paths
  path#0  value -> binding pat#0
```

```
BODY fun size#1 in module #0

root expr#0
params
  pat#0
exprs
  expr#0  path path#0
pats
  pat#0  bind value
paths
  path#0  value -> binding pat#0
```

## Lowering Diagnostics

296..333: `fun size` is declared more than once in this module
347..357: `type Point` is declared more than once in this module

## Parse Diagnostics

No diagnostics.
