---
source: crates/mlkc-lower/tests/spec_test.rs
expression: snapshot
---
## Input

```mlk
// A negative number: the language has a minus operator and no sign, so `-42` is an
// expression the parser cannot read, and the body of the function holds a missing expression.
// Giving the language a prefix `-` needs a unary expression in the HIR as well.

@builtin
type Int

fun negative(): Int =
    -42

```

## Item Tree

```
MODULE #0

ITEM TREE
  type Int  @2.0
    attributes: @builtin
    visibility: private
  fun negative  @2.1
    visibility: private
    ret: Int -> type Int
```

## Bodies

```
BODY fun negative in module #0

root expr#0
exprs
  expr#0  missing
```

## Lowering Diagnostics

No diagnostics.

## Parse Diagnostics

306..307: Expected an expression but instead found '-'.
