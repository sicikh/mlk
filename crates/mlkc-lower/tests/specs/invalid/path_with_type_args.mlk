// A path of the project carrying a type argument: what a module is named by and what an
// import names is a path of names, and an argument written at a segment --- the root of the
// path included --- is what a type is applied to.

module project.main[Int]

use std[Int].core

use project.data.Map[Int, String]

fun main(): Int =
    1
