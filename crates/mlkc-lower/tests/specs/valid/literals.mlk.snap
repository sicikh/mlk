---
source: crates/mlkc-lower/tests/spec_test.rs
expression: snapshot
---
## Input

```mlk
// The shape of a literal: an integer, a string, and a subtraction, which is an operator
// rather than the sign of a number. What a string holds is what its escapes decode to.

@builtin
type Int

fun positive(): Int =
    42

fun text(): String =
    "hello"

fun escaped(): String =
    "a \"quote\", a \\ backslash, a line break \n, and a tab \t"

fun subtracted(): Int =
    1 - 2

```

## Item Tree

```
MODULE #0

ITEM TREE
  type Int  @2.0
    attributes: @builtin
    visibility: private
  fun positive  @2.1
    visibility: private
    ret: Int -> type Int
  fun text  @2.2
    visibility: private
    ret: String -> unresolved
  fun escaped  @2.3
    visibility: private
    ret: String -> unresolved
  fun subtracted  @2.4
    visibility: private
    ret: Int -> type Int
  use Int  prelude
    path: std.prelude.Int
    visibility: private
  use Unit  prelude
    path: std.prelude.Unit
    visibility: private
```

## Bodies

```
BODY fun positive in module #0

root expr#0
exprs
  expr#0  literal 42
```

```
BODY fun text in module #0

root expr#0
exprs
  expr#0  literal "hello"
```

```
BODY fun escaped in module #0

root expr#0
exprs
  expr#0  literal "a \"quote\", a \\ backslash, a line break \n, and a tab \t"
```

```
BODY fun subtracted in module #0

root expr#2
exprs
  expr#0  literal 1
  expr#1  literal 2
  expr#2  binary expr#0 - expr#1
```

## Lowering Diagnostics

No diagnostics.

## Parse Diagnostics

No diagnostics.
