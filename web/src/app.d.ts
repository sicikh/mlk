// The types the application is allowed to declare for itself.
//
// There is nothing here yet: the editor is a page and a wasm module,
// and both of them carry their types with them.

declare global {
	namespace App {}
}

export {};
